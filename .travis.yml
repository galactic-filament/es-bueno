sudo: false
language: node_js
node_js:
  - 4.1
services:
  - postgresql

env:
  - DATABASE_HOST=localhost
before_install:
  - psql -f ./db/container-files/docker-entrypoint-initdb.d/db.sql -U postgres
  - cd ./app
before_script:
  - npm install --silent mocha
after_success:
  - coveralls
